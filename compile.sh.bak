#!/bin/bash
echo "Compiling SSB modules..."

# 64-bit compile
echo "🔨 Compiling 64-bit version..."
cython -3 ssb_64.pyx
gcc -shared -fPIC -O2 -I/data/data/com.termux/files/usr/include/python3.11 ssb_64.c -o ssb_64.so

# 32-bit compile
echo "🔨 Compiling 32-bit version..."  
cython -3 ssb_32.pyx
gcc -shared -fPIC -m32 -O2 -I/data/data/com.termux/files/usr/include/python3.11 ssb_32.c -o ssb_32.so

echo "✅ Both versions compiled successfully!"
ls -la *.so